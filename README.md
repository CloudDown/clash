# clash

Small C++ board prototype: place pieces on an 8×5 grid (8 rows, 5 columns), then print the board.

```
g++ -o clash main.cpp plateau.cpp
./clash
```

`main.cpp` includes `plateau.cpp`; keep both files in the same directory.
